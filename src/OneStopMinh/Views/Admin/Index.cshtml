@model IEnumerable<OneStopMinh.Models.ApplicationUser>

<h5>All users</h5>

    <ul>
@foreach (var user in Model)
{
    <li>@user.UserName</li>
}
    </ul>


@Html.ActionLink("Role Console", "Index", "Roles")